# Local Backline runtime config. The Cloudflare build generates the deployed one.
supabase-config.js

# Local dependency/cache folders.
node_modules/
.DS_Store
Thumbs.db

# Cloudflare build output. tools/build-public-site.mjs generates the app copy.
public-site/app/

# Legacy static deployment output.
_site/
