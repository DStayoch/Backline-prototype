# Local Backline runtime config. GitHub Pages generates this during deployment.
supabase-config.js

# Local dependency/cache folders.
node_modules/
.DS_Store
Thumbs.db

# Static deployment output generated by the GitHub Pages workflow.
_site/
